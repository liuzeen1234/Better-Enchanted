# 项目规则

- 不要主动修改 `DEV_PLAN.md`，除非用户明确要求。
- 不要主动执行 `git commit`，除非用户在对话中明确要求提交代码。
- 不要主动执行 `git push`，除非用户在对话中明确要求推送代码。
- 当用户要求"记住"某条规则时，必须将其记录到 `rules.md` 中。
- 在未经用户允许的情况下，不能删除没有被 git 跟踪的文件，也不能运行会删除未被 git 跟踪的文件的命令（如 `git clean`）。如果必须删除，应先向用户确认并说明可能被删除的文件。
